<!DOCTYPE html>
<html lang="en">
  <%-include("./partials/head.ejs")%>
<body>
    <%-include("./partials/nav.ejs")%>
      <div class="employee-content">
        <h2>Customers Reviews</h2>
        <% if (result.length>0){%>
          <% result.forEach(e =>{ %>
             
            <h3>Book title: <%=e.bookTitle%> </h3>
            <h3> Review by: <%=e.Name%></h3>

            <h4 style="color: crimson;"> Reviews Content:<%=e.reviews%></h4>
          
            <%})%>
       <% } else {%>
        <p>File empty</p>
        <%}%>

      </div>
      
</body>
<%-include("./partials/footer.ejs")%>
</html>